# This file was autogenerated by actions.nix. Do not edit it manually.
# To make changes, edit the workflow definition in your flake's actions-nix configuration
# (typically under flake.actions-nix.workflows.".github/workflows/ci.yml") and run:
#   nix run .#render-workflows
# Or commit to trigger the pre-commit hook if enabled.
jobs:
  eval:
    runs-on: ubuntu-24.04-arm
    steps:
    - uses: actions/checkout@v6
    - uses: cachix/install-nix-action@v31
    - name: Evaluate
      run: |-
        nix flake check \
          --all-systems \
          --no-build \
          --no-write-lock-file
  smoke-linux:
    needs:
    - eval
    runs-on: ubuntu-24.04-arm
    steps:
    - uses: actions/checkout@v6
    - uses: cachix/install-nix-action@v31
    - name: Build representative Linux targets
      run: |-
        nix build \
          ".#checks.aarch64-linux.build-nixos-aarch64-linux-a--theme-light--session-gui" \
          ".#checks.aarch64-linux.build-home-aarch64-linux-a--account-a--theme-light--session-gui" \
          --no-write-lock-file
name: CI
'on':
  pull_request: {}
  push:
    branches:
    - main
  workflow_dispatch: {}
