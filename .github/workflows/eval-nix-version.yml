# This file was autogenerated by actions.nix. Do not edit it manually.
# To make changes, edit the workflow definition in your flake's actions-nix configuration
# (typically under flake.actions-nix.workflows.".github/workflows/eval-nix-version.yml") and run:
#   nix run .#render-workflows
# Or commit to trigger the pre-commit hook if enabled.
jobs:
  eval-nix-version:
    runs-on: ubuntu-24.04-arm
    steps:
    - uses: actions/checkout@v6
      with:
        persist-credentials: false
    - uses: cachix/install-nix-action@v31
      with:
        install_url: https://releases.nixos.org/nix/nix-${{ matrix.nix_version }}/install
    - name: Show Nix versions
      run: |-
        nix --version
        nix store info --json
    - name: Evaluate NixOS target directly
      run: |-
        nix flake check \
          --all-systems \
          --no-build \
          --no-write-lock-file
        nix eval --raw \
          ".#nixosConfigurations.aarch64-linux-a--theme-light--session-gui.config.system.build.toplevel.drvPath"
    strategy:
      fail-fast: false
      matrix:
        nix_version:
        - 2.34.8
        - 2.35.2
    timeout-minutes: 45
name: temporalily diagnostics
'on':
  pull_request: {}
