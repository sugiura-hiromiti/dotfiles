# This file was autogenerated by actions.nix. Do not edit it manually.
# To make changes, edit the workflow definition in your flake's actions-nix configuration
# (typically under flake.actions-nix.workflows.".github/workflows/eval-nix-version.yml") and run:
#   nix run .#render-workflows
# Or commit to trigger the pre-commit hook if enabled.
jobs:
  eval-nix-version:
    runs-on: ubuntu-24.04-arm
    steps:
    - uses: actions/checkout@v6
      with:
        persist-credentials: false
    - uses: cachix/install-nix-action@v31
      with:
        install_url: https://releases.nixos.org/nix/nix-${{ matrix.nix_version }}/install
    - name: Evaluate ready NixOS targets
      run: |-
        nix eval --no-update-lock-file --option allow-import-from-derivation false --raw .#checks.aarch64-linux.installer-e2e.drvPath
        nix flake check --no-build --no-update-lock-file .
        nix eval --no-update-lock-file --raw .#nixosConfigurations.aarch64-linux-a--theme-dark--session-gui.config.system.build.toplevel.drvPath
        nix eval --no-update-lock-file --raw .#nixosConfigurations.aarch64-linux-a--theme-dark--session-tty.config.system.build.toplevel.drvPath
        nix eval --no-update-lock-file --raw .#nixosConfigurations.aarch64-linux-a--theme-light--session-gui.config.system.build.toplevel.drvPath
        nix eval --no-update-lock-file --raw .#nixosConfigurations.aarch64-linux-a--theme-light--session-tty.config.system.build.toplevel.drvPath
    strategy:
      fail-fast: false
      matrix:
        nix_version:
        - 2.34.8
        - 2.35.2
    timeout-minutes: 120
name: Nix evaluation compatibility
'on':
  pull_request: {}
